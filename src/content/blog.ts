/**
 * @deprecated Static posts removed — blog data comes from the public Blog API.
 * Types live in `@/types/blog`; fetch via `@/lib/blog-api` or `@/lib/posts`.
 */
export type {
  BlogPost,
  BlogAuthor,
  BlogContentBlock,
  BlogLink,
  BlogCategory,
} from '@/types/blog';
