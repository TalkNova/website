# Legal pages content

Source documents (edit these, then regenerate JSON):

- `privacy-policy.html` (repo root or `Downloads/privacy-policy.html`)
- `terms-and-conditions.html` (repo root or `Downloads/terms-and-conditions.html`)

Rendered at:

- `/privacy-policy` (alias `/privacy`)
- `/terms-and-conditions` (alias `/terms`)

Regenerate JSON after editing HTML:

```bash
npm run legal:extract
```
