/** Stable blog dates for SSR and listing (avoids locale/timezone drift). */
export function formatPostDate(iso: string) {
  return new Date(iso).toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
    timeZone: 'UTC',
  });
}
